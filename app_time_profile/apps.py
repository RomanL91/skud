from django.apps import AppConfig


class AppTimeProfileConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "app_time_profile"
    verbose_name = "Профиля доступа по времени"
